<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/rlTitle"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_tab_height">

        <RadioGroup
            android:id="@+id/rgMainTab"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/colorAccent"
            android:gravity="center"
            android:orientation="horizontal">

            <RadioButton
                android:id="@+id/rbRandom"
                android:layout_width="@dimen/main_tab_icon_size"
                android:layout_height="@dimen/main_tab_icon_size"
                android:background="@drawable/selector_main_tab_random"
                android:button="@null"
                android:checked="true" />

            <RadioButton
                android:id="@+id/rbList"
                android:layout_width="@dimen/main_tab_icon_size"
                android:layout_height="@dimen/main_tab_icon_size"
                android:layout_marginLeft="@dimen/main_tab_margin"
                android:layout_marginRight="@dimen/main_tab_margin"
                android:background="@drawable/selector_main_tab_list"
                android:button="@null" />

            <RadioButton
                android:id="@+id/rbUser"
                android:layout_width="@dimen/main_tab_icon_size"
                android:layout_height="@dimen/main_tab_icon_size"
                android:background="@drawable/selector_main_tab_user"
                android:button="@null" />
        </RadioGroup>

        <ImageButton
            android:id="@+id/ibtnSearch"
            android:layout_width="@dimen/main_tab_height"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:padding="@dimen/main_tab_search_btn_padding"
            android:scaleType="centerInside"
            android:src="@drawable/selector_main_search_btn" />
    </RelativeLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/vpContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/rlTitle" />

</RelativeLayout>
